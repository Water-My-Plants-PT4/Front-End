import moduleName from '../actions';

const initialState = {
  username: '',
  password: '',
  phonenumber: '',
};

export const reducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
